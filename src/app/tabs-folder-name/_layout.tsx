import { Tabs } from 'expo-router';

export default function TabsFolderNameLayout() {
    return (
        <Tabs
            screenOptions={{
                headerShown: false
            }}
        ></Tabs>
    );
}
